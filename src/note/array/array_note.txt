FirstMissingPositvie41
     排序思想
    // the key point : makes nums[i] = i
       // that operation costs constant time to swap two number, and sorts the nums

    考虑逆序的情况,则是 nums[i] = len - i

MiniumSizeSubarraysum 209
  一个java库二分查找方法的使用
  int result = Arrays.binarySearch(cs, cumuSum);  // actual index or should-be index

  index of the search key, if it is contained in the array;
       *         otherwise, <tt>(-(<i>insertion point</i>) - 1)</tt>.  The
       *         <i>insertion point</i> is defined as the point at which the
       *         key would be inserted into the array: the index of the first
       *         element greater than the key, or <tt>a.length</tt> if all
       *         elements in the array are less than the specified key.


 Range Sum Query 303
   运用累积和数组