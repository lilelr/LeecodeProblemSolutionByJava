Implement str Str 28
字符串匹配 KMP O(m+n)

Merge Sorted Array 88
合并两个有序的数组.
两个指针,各指向两个数组的末尾,从后往首scan.
 // Since we have assume that nums1 have enough space (size that is equal er greater than m+n),
    // iterate backforward using two points is simple and easy.


Minimum Window Substring 76

两个指针 i和j ,先是j向前移动,再是i向前移动. 两个指针之间的一段元素之间维持states平衡,详见explanation


Minimum Size Subarray Sum 209
 For example, given the array [2,3,1,2,4,3] and s = 7,
 the subarray [4,3] has the minimal length under the problem constraint.
 从一个序列中找满足给定的和最短长度的连续子序列
 双指针i和start,加上动态规划的思想,动态地变化start指针,计算子序列的和 以及最短长度
 while (sum - nums[start] >= s && start < len) {
                 sum -= nums[start];
                 start++;
                 min = Math.min(min, i - start + 1);
             }



Remove Duplicates from Sorted Array 26
两个指针,一前一后,前指针保留不同元素的index




Rotate Array 189

rotate and rotate again

Rotate List 61
双指针在链表中的应用,一般会添加dump 节点,p,pre都从dump节点开始scan. 注意优先处理head为null或head->next = null 的情况


Sum 15
排序之后,
需要三个指针时,一个p1 局外的递增, 另外两个  start 和 end 一首一尾,逐渐合拢

Sum 18
kSum 问题通解, 基本思路是 两个指针的话则是 start 和end ,里面用do .. while ..循环处理
k个指针的话,则是以双指针为基础,在外围慢慢增多指针

Sum Closet 16
三指针问题, 用




